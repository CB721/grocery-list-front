module.exports = {
    // return if the application is online or not
    isOnline: () => {
        return navigator.onLine;
    },
    saveToIndexedDB: (data) => {
        // what route
    },
    sendIndexedDB: () => {

    }
}